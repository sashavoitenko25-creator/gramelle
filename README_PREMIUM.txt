Dice premium UI overlay

1. Unpack over project
2. Ensure supabase/dice.sql already applied
3. Deploy

Includes:
- DiceScreen (lobby, create, table, history, avatars, dice animation, sounds)
- GamesScreen card with online counter
- page.tsx wiring + RPS-like props
- globals.css dice animations
- BottomNav active state for dice
